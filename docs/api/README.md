# docs/api

Not populated yet. `apps/api` currently exposes only `GET /health`. API documentation
(REST/gRPC surfaces, WebSocket/SSE channels) is added here as real endpoints land,
starting with the Phase 1 market-data read paths.
