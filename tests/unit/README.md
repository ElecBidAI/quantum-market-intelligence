# tests/unit

Cross-cutting unit tests that don't belong to a single package/app. Phase 0 tests
live next to their source (`packages/*/test`, `packages/quant-core/tests`) instead;
this directory is reserved for later cross-package suites.
